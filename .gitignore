# OS generated files
.DS_Store
.AppleDouble
.LSOverride

# WordPress Defaults
wp-content/uploads/
wp-content/cache/
wp-content/upgrade/
wp-content/backups/
wp-content/aiowps_backups/

# Project specific backups
wp-backup/
*.zip
*.gz
*.sql
*.tar

# Logs
*.log
wp-content/debug.log

# Docker
# We want to keep docker-compose.yml but maybe ignore temporary docker data if any mapped (here we mapped . to /var/www/html so no extra volumes except db_data which is named volume)

# Secrets?
# wp-config.php contains real creds. Usually ignored.
# For this local convenience, user might want it, but standard practice is ignore.
# I will include it for now as per user request to "manage this", but normally we wouldn't.
# If I ignore it, they lose the docker config I just added.
# I will NOT ignore wp-config.php for this specific "local preview" snapshot request, unless user says otherwise.
