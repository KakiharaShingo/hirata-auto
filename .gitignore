# OS generated files
.DS_Store
.AppleDouble
.LSOverride

# WordPress Defaults
wp-content/uploads/
wp-content/cache/
wp-content/upgrade/
wp-content/backups/
wp-content/aiowps_backups/

# Project specific backups
wp-backup/
*.zip
*.gz
*.sql
*.tar

# Logs
*.log
wp-content/debug.log

# Docker
# We want to keep docker-compose.yml but maybe ignore temporary docker data if any mapped (here we mapped . to /var/www/html so no extra volumes except db_data which is named volume)

# Secrets - Public repoなのでwp-configは除外
wp-config.php
wp-config---.php
wp-config--.php
wp-config-.php
